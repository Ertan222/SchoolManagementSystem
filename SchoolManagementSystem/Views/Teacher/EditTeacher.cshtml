@model TeacherViewModel
@{
    ViewData["Title"] = "EditTeacher";
    Layout = "~/Views/Shared/_Layout.cshtml";
}




<form asp-controller="Teacher" asp-action="EditTeacher" asp-route-id="@Model.Teacher.HumanId" method="post">
    <div asp-validation-summary="All" class="text-danger"></div>
    <div class="row">
        <div class="col-12">
            <h5 class="form-title"><span>Edit Teacher</span></h5>
        </div>
        <div class="col-12 col-sm-6">
            <div class="form-group">
                <label>Teacher Full Name</label>
                <input type="text" name="FirstandSecondName" class="form-control" value="@Model.Teacher.FirstandSecondName">
            </div>
        </div>
        <div class="col-12 col-sm-6">
            <div class="form-group">
                <label>Last Name</label>
                <input type="text" name="Lastname" value="@Model.Teacher.Lastname" class="form-control">
            </div>
        </div>
        <div class="col-12 col-sm-6">
            <div class="form-group">
                <label>Class</label>
                <select class="form-control" name="ClasssId">
                    <option selected value="0">Select Class</option>
                    @foreach (Classs classs in Model.ClasssList)
                    {
                        <option selected="@(classs.ClasssId == Model.Teacher.ClasssId)" value="@classs.ClasssId">@classs.ClasssName</option>
                    }
                </select>
            </div>
        </div>
        <div class="col-12 col-sm-6">
            <div class="form-group">
                <label>Gender</label>
                <select class="form-control" name="GenderId">
                    <option selected value="0">Select Gender</option>
                    @foreach (Gender gender in Model.GenderList)
                    {
                        <option selected="@(gender.GenderId == Model.Teacher.GenderId)" value="@gender.GenderId">@gender.GenderName</option>
                    }
                </select>
            </div>
        </div>
        <div class="col-12 col-sm-6">
            <div class="form-group">
                <label>Date of Birth</label>
                <input type="date" name="DOB" class="form-control" value="@(Model.Teacher.DOB.Year !=1?Model.Teacher.DOB.ToString("yyyy-MM-dd"):"")">
            </div>
        </div>

        <div class="col-12 col-sm-6">
            <div class="form-group">
                <label>Email</label>
                <input type="text" name="Email" value="@Model.Teacher.Email" class="form-control">
            </div>
        </div>
        <div class="col-12 col-sm-6">
            <div class="form-group">
                <label>Phone Number</label>
                <input type="text" name="PhoneNumber" value="@Model.Teacher.PhoneNumber" class="form-control">
            </div>
        </div>
        <div class="col-12 col-sm-6">
            <div class="form-group">
                <label>Address</label>
                <textarea name="Address" cols="10" rows="10" style="resize:none; margin:auto;">@Model.Teacher.Address</textarea>
            </div>
        </div>
        <div class="col-12 col-sm-6">
            <div class="form-group">
                <label>Join Date</label>
                <input type="date" name="JoinDate" class="form-control" value="@(Model.Teacher.JoinDate.Year !=1?Model.Teacher.JoinDate.ToString("yyyy-MM-dd"):"")">
            </div>
        </div>
        <div class="col-12">
            <button type="submit" class="btn btn-primary">Edit</button>
        </div>
    </div>
</form>


